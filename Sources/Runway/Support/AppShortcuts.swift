import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Toggles the menu-bar popover from anywhere. No default combo — the user records one in the
    /// Settings window (the recorder field clears with its ⓧ, which disables the shortcut).
    static let togglePopover = Self("togglePopover")
}
